{
  "name": "@pay-skill/ows-policy",
  "version": "0.1.0",
  "description": "OWS policy executable for Pay — chain lock, contract allowlist, per-tx and daily spending caps",
  "type": "module",
  "main": "./dist/pay-policy.js",
  "bin": {
    "pay-policy": "./dist/pay-policy.js"
  },
  "scripts": {
    "build": "tsc",
    "test": "node --import tsx --test tests/*.ts",
    "typecheck": "tsc --noEmit"
  },
  "devDependencies": {
    "@types/node": "^25.5.2",
    "tsx": "^4.0.0",
    "typescript": "^6.0.2"
  },
  "engines": {
    "node": ">=20.0.0"
  },
  "license": "MIT",
  "files": [
    "dist/**/*.js",
    "dist/**/*.d.ts"
  ],
  "keywords": [
    "pay",
    "pay-skill",
    "ows",
    "open-wallet-standard",
    "policy",
    "usdc",
    "base"
  ]
}
